import 'package:flutter/material.dart';

const mystyle= TextStyle(
                      fontWeight: FontWeight.bold,
                       );
const mystyle1=TextStyle(
                    fontWeight:FontWeight.w700,           
                  );
const mystylewithsize=TextStyle(
                         color: Colors.green,
                         fontSize:24.0,      
                        fontWeight:FontWeight.bold,                  
                    );                  
const mycardstyle=TextStyle(
                         color: Colors.blue,
                         fontSize:40.0,      
                        fontWeight:FontWeight.bold,                  
                    );  
const myaccountstyle=TextStyle(
                         color: Colors.amber,
                         fontSize:40.0,      
                        fontWeight:FontWeight.bold,                  
                    );  
 const mycardwithcolor=TextStyle(
                         //color: Colors.blue,
                         fontSize:50.0,      
                        fontWeight:FontWeight.bold,                  
                    );                                      